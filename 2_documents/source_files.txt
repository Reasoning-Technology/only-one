types.h  utils.h

directory.h
  code to walk a directory tree, various pathname manipulation routines

file.h  
  code to characterize and compare files

parse_status.h  
  enum of return flags for parsing

phrase.h 
  the controlled string token and phrase definition

taxonomy.h  
  defines a node_set:  a node phrase, followed by one or more source phrases, then a blank line
  a map of nodes
  the node number allocator
